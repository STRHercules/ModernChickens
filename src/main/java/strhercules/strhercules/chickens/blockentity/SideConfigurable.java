package strhercules.chickens.blockentity;

/** Implemented by machines that can be configured with the in-game configurator. */
public interface SideConfigurable {
    MachineSideConfig sideConfig();
}
